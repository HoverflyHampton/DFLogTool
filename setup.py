#!/usr/bin/env python


from setuptools import setup, find_packages

setup(name='log_parser',
      version='1.2.12',
      description='Python DF Log Merge and Parsing Tool',
      author='William Hampton',
      packages=find_packages(),
     )